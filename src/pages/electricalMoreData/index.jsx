import React, { memo } from 'react';

const ElectricalMoreData = () => {
    return (
        <div>
            <h1>Electrical More Data</h1>
        </div>
    );
};

export default memo(ElectricalMoreData);